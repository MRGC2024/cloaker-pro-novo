# Railway — healthcheck evita "Application failed to respond" quando o boot demora
[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
